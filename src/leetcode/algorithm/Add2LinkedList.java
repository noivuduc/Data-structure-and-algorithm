package leetcode.algorithm;

public class Add2LinkedList {
    class Node {
        int value;
        Node next;
        Node(int value) {
            this.value = value;
        }
    }

    private int getSize(Node head) {
        int size = 0;
        return size;
    }
}
